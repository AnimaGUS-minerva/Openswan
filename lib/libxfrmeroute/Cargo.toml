[package]
name = "libxfrmeroute"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type      = ["staticlib"]

[dependencies]
rtnetlink     = { features = [ "tokio_socket" ], version = "0.11.*" }
